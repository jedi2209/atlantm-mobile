import {APP_LANG_SET} from './actionTypes';

export const actionSetGlobalLanguage = (lang) => {
    return alert(lang);
//   return (dispatch) => {
//     dispatch({
//       type: APP_LANG_SET,
//       payload: lang,
//     });
//   };
};
